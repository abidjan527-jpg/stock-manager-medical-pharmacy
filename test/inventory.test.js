const test=require('node:test'),assert=require('node:assert/strict');require('../inventory.js');const{daysUntilExpiry,statusOf,adjust,validate}=globalThis.InventoryCore,now=new Date(2026,8,5);test('calculates month-end expiry',()=>assert.equal(daysUntilExpiry('2026-09',now),26));test('prioritizes expired and out-of-stock alerts',()=>{assert.equal(statusOf({expiry:'2026-08',quantity:10,reorderLevel:2},now),'expired');assert.equal(statusOf({expiry:'2027-12',quantity:0,reorderLevel:2},now),'out')});test('flags expiry and low stock',()=>{assert.equal(statusOf({expiry:'2026-11',quantity:20,reorderLevel:2},now),'expiring');assert.equal(statusOf({expiry:'2027-12',quantity:2,reorderLevel:2},now),'low')});test('stock movements enforce balance',()=>{const x={quantity:5,history:[]};assert.equal(adjust(x,'in',3).quantity,8);assert.equal(adjust(x,'out',2).quantity,3);assert.throws(()=>adjust(x,'out',6),/Not enough stock/)});test('rejects duplicate batch',()=>assert.match(validate({id:'2',name:'Panadol',batch:'A1',expiry:'2027-01'},[{id:'1',name:'panadol',batch:'a1'}]),/already exist/));
